'use strict';

const allDuckPics = []; //this is the state or important data to this app

function DuckPic(name, source) {
  this.timesClicked = 0;
  this.source = source;
}

allDuckPics.push(new Image(') 